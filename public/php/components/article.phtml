<div class="upperPrint">
  <h1>{{title}}</h1>
  <img src="{{articleImgPath}}" alt="{{altImg}}">
</div>
<p>{{articleContent}}</p>
</div>
<div class="info_art">
    <div id="author">
        <img src="{{authorImg}}">
        <p>{{authorName}}</p>
        <p>{{authorEmail}}</p>
    </div>
    <div id="get_cat">
        <!-- tag che viene sostituito dalle categorie a cui appartiene l'articolo nb=> check se sono settate altrimenti messaggio che non ci sono cateegorie-->
        <categories />
    </div>
